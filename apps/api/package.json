{
  "name": "api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently -k -n fastapi,auth -c blue,magenta \"UV_CACHE_DIR=.uv-cache python3 -m uv run fastapi dev app/main.py --host 0.0.0.0 --port 8000\" \"node --watch --env-file-if-exists=.env --import tsx auth/server.ts\"",
    "dev:fastapi": "UV_CACHE_DIR=.uv-cache python3 -m uv run fastapi dev app/main.py --host 0.0.0.0 --port 8000",
    "dev:auth": "node --watch --env-file-if-exists=.env --import tsx auth/server.ts",
    "start": "concurrently -k -n fastapi,auth -c blue,magenta \"UV_CACHE_DIR=.uv-cache python3 -m uv run fastapi run app/main.py --host 0.0.0.0 --port 8000\" \"node --env-file-if-exists=.env --import tsx auth/server.ts\"",
    "start:fastapi": "UV_CACHE_DIR=.uv-cache python3 -m uv run fastapi run app/main.py --host 0.0.0.0 --port 8000",
    "start:auth": "node --env-file-if-exists=.env --import tsx auth/server.ts",
    "db:migrate": "UV_CACHE_DIR=.uv-cache python3 -m uv run alembic upgrade head",
    "db:revision": "UV_CACHE_DIR=.uv-cache python3 -m uv run alembic revision --autogenerate",
    "lint": "UV_CACHE_DIR=.uv-cache python3 -m uv run ruff check .",
    "format": "UV_CACHE_DIR=.uv-cache python3 -m uv run ruff format .",
    "test": "UV_CACHE_DIR=.uv-cache python3 -m uv run pytest"
  },
  "dependencies": {
    "@hono/node-server": "^1.19.9",
    "better-auth": "^1.6.0",
    "hono": "^4.11.4",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "@types/pg": "^8.15.6",
    "concurrently": "^9.2.1",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
