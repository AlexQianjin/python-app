{
  "name": "api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "UV_CACHE_DIR=.uv-cache python3 -m uv run fastapi dev app/main.py --host 0.0.0.0 --port 8000",
    "start": "UV_CACHE_DIR=.uv-cache python3 -m uv run fastapi run app/main.py --host 0.0.0.0 --port 8000",
    "lint": "UV_CACHE_DIR=.uv-cache python3 -m uv run ruff check .",
    "format": "UV_CACHE_DIR=.uv-cache python3 -m uv run ruff format .",
    "test": "UV_CACHE_DIR=.uv-cache python3 -m uv run pytest"
  }
}
